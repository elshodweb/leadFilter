# Server Port
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://admin:password123@localhost:27017/lead_filter?authSource=admin
MONGODB_DB_NAME=lead_filter

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini

# JWT Authentication
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d


# Generate two distinct secrets: openssl rand -hex 32
# Local demo data only; never enabled in production
SEED_DEMO_DATA=false
